# Cursor Hooks

Hook scripts referenced from `.cursor/hooks.json` (paths relative to project root).

Docs: https://cursor.com/docs/hooks.md

## Add one

1. Copy `hooks.json.example` to `hooks.json`
2. Add scripts under `.cursor/hooks/`
3. Point hook entries at those scripts
