# Cursor Skills

On-demand Agent Skills. Each skill is a folder with required `SKILL.md`.

Docs: https://cursor.com/docs/skills.md

## Add one

```text
.cursor/skills/my-skill/
  SKILL.md
  scripts/
  references/
  assets/
```

Frontmatter requires `name` (must match folder) and `description`.
