# Kiro Custom Agents

Custom agent profiles for this repo (`.json` or `.md`).

Docs: https://kiro.dev/docs/custom-agents/

## Add one

Add `.kiro/agents/<name>.json` (or `.md`). Point `resources` at steering if needed, e.g. `file://.kiro/steering/**/*.md`.
