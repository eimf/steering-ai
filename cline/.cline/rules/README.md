# `.cline/rules/`

Canonical Cline project rules live in **`.clinerules/`** at the project root.

Keep this folder for the newer `.cline/` config tree, or symlink/copy from `.clinerules/`.

Docs: https://docs.cline.bot/customization/cline-rules.md
